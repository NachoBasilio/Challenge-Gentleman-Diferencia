SELECT DISTINCT * FROM users;

-- La palabra clave DISTINCT asegura que los resultados no contengan filas duplicadas. Es decir, elimina cualquier duplicado en las filas seleccionadas.

SELECT DISTINCT age FROM users

-- Por ejemplo, en este caso solo nos traería las edades que no se repitan, las que se repiten solo la traería una vez.

