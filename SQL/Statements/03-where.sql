SELECT * FROM users WHERE age=26

-- Con el WHERE vamos a limitar el criterio con el que vamos a recuperar los datos.
-- En el ejemplo solo nos traería los usuarios que cumplan con que la edad sea 26

SELECT name FROM users WHERE age=26

-- En este caso me traería los nombres de las personas que tengan 26 años.