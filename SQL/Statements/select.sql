SELECT * FROM users;

-- Select nos sirve para elegir que queremos traer de una tabla en especifico. El asterisco hace referencia a todos los campos, aunque podemos especificar. Por ejemplo:

SELECT name FROM users;

SELECT user_id, name FROM users;