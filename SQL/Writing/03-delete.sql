DELETE FROM users WHERE user_id = 27;

-- Con el DELETE se borra el registro de la tabla. SIEMPRE se debe tener cuidado con el DELETE ya que no se puede recuperar la información. El WHERE es importante para indicar que registro se va a borrar, no se debe olvidar.
