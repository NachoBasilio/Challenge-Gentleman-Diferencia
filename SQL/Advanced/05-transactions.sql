-- Las transacciones son un conjunto de operaciones que se ejecutan como una sola unidad. Es decir, si una de las operaciones falla, todas las operaciones se deshacen y la base de datos queda en el mismo estado en el que estaba antes de que se ejecutara la transaccion.

START TRANSACTION
COMMIT
ROLLBACK
